RewriteOptions inherit
<IfModule mod_rewrite.c>

RewriteEngine on
RewriteCond %{HTTP_HOST} ^www.Cplanner.com
RewriteRule (.*) https://Cplanner.com/$1 [R=301,L]

    RewriteEngine On
    RewriteRule ^(.*) public/$1 [L]
</IfModule>
